export const fetchGitHubStats = async (username: string) => {
  // Placeholder for GitHub API fetching logic
  console.log(`Fetching stats for ${username}...`);
  return {
    repos: 0,
    followers: 0,
  };
};
